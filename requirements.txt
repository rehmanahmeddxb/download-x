# Minimal, pinned, and COMPLETE: every runtime dependency, including
# transitive pure-Python ones, must be listed here AND in buildozer.spec.
# p4a cannot auto-resolve packages without Android wheels (notably
# SQLAlchemy), so an unlisted dep would silently be missing from the APK
# and crash the app on launch with ModuleNotFoundError.
#
# Deliberately absent (verified unused): Kivy (webview bootstrap needs no
# UI toolkit), waitress (Flask's threaded dev server is enough for a
# loopback server), requests (direct streaming uses stdlib urllib),
# APScheduler (nothing schedules jobs).
#
# Bump yt-dlp periodically -- YouTube changes break old versions regularly.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
typing-extensions==4.16.0
yt-dlp==2026.8.19
mutagen==1.48.1
certifi==2026.7.22
